---

version: "3"

services:
  mongo-db-nestjs-coffee:
    image: mongo # container image to be used
    restart: always 
    ports: # expose ports in “host:container” format
      - 27020:27017
    environment: #env variables to pass into the container
       MONGODB_DATABASE: nest-course